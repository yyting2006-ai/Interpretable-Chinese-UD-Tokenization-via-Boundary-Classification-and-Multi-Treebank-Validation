# Data Directory

This directory is intentionally left empty in the Git repository.

Running

```bash
python scripts/run_experiments.py
```

downloads the required Universal Dependencies Chinese treebank files into `data/ud/`.

The default release tag is `r2.18`.

Raw UD treebanks are licensed by Universal Dependencies under CC BY-SA 4.0 and are not redistributed in this repository.

